# Matriz Inversa
- Uma matriz é invesível se existe $ A^{-1} $ tal que:
$$ A \cdot A^{-1} = A^{-1} \cdot A = I $$
- Para ser inversível, o determinante da matriz tem que ser diferente de zero. 
    - Ou seja, a matriz tem que ser não-singular.
    - Se a matriz for singular, não será inversível.